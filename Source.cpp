#include <iostream>
#include <conio.h>
#include <iomanip>
#include <Windows.h>
using namspace std;

int main() {
	int number = 0;
	do
	{
		cout << "Enter a number between 0 and 10: ";
		cin >> number;
		if (number < 0 || number > 10)
		{
			cout << ""
		}
	}
}
